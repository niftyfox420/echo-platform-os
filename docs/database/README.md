# Database Documentation

Document every custom table, option, post meta key, and migration here before changing schemas.

Each table document should include purpose, columns, indexes, retention behavior, migration version, and uninstall behavior.
