# API Documentation

This folder documents Echo Platform OS REST endpoints, permissions, request schemas, response schemas, and webhook events.

Never include live credentials or private supplier URLs in examples.
