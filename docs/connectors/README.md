# Connector SDK

Connectors adapt supplier systems to the shared sync engine.

A connector may support discovery, authentication, connection testing, catalog fetch, pagination, rate limits, field mapping, preview, and webhooks.

Manual CSV, JSON, and XML import must remain available when no API exists.
